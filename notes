# install nodejs with pacman which also should install npm
# create directory and go to working directory
npm init
npm install -S webpack
# do also this to run it globally
npm install -G webpack
create scripts.js -file
touch webpack.config.js
webpack
